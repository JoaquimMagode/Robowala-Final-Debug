{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "cp prisma/schema.production.prisma prisma/schema.prisma && prisma generate && next build",
  "env": {
    "NEXTAUTH_URL": "https://robowala-final.vercel.app"
  }
}